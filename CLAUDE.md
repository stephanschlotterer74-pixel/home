# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently empty — there are no source files, build scripts, or established conventions yet. This CLAUDE.md should be updated once the project takes shape.

When the project is initialized, update this file to include:

- **Build / lint / test commands** (e.g. `npm run build`, `pytest`, `make test`)
- **How to run a single test** in isolation
- **High-level architecture** — how the major pieces fit together, data flow, key abstractions
- **Non-obvious conventions** — naming rules, branch strategy, code generation steps, or anything that would surprise a new contributor
